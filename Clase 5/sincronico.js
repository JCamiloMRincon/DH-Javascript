// Function: la maquinita
// Parámetro: valor
// return

function saludar(nombre) { // Declaración de la función.
    return "Hola " + nombre
}

console.log( saludar("Camilo") ); // Invocación de la función.

console.log( Math.random() ); // Math.random() muestra números aleatorios entre 0 y 1.

// Ejemplo de función matemática
function f(x) {
    return 2*x+5
}

console.log(f(0.5));